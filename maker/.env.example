# Copy to maker/.env (gitignored) and fill in. The maker reads its key ONLY from
# here — never from your global shell — so generating a release bills the Anthropic
# API directly and never touches Claude Code's OAuth/Max billing.
ANTHROPIC_API_KEY=sk-ant-...
